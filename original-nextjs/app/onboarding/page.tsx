"use client"; import { useState } from "react";
const steps=["Basic profile","Professional links","Import sources","Documents","Review extraction","Professional positioning","Website preferences","Review & publish"];
export default function Onboarding(){const [step,setStep]=useState(0);return <main className="onboarding"><p className="eyebrow">Step {step+1} of 8</p><div className="progress"><i style={{width:`${(step+1)/8*100}%`}}/></div><h1>Let&apos;s build your professional presence.</h1><h2>{steps[step]}</h2><p>Everything is optional except your name and headline. You can skip this step and edit it later.</p><label>Notes or details<input placeholder="Add details when ready"/></label><div className="actions"><button onClick={()=>setStep(Math.max(0,step-1))} disabled={!step}>Back</button><button className="quiet" onClick={()=>setStep(Math.min(7,step+1))}>{step===7?"Finish setup":"Skip for now"}</button></div></main>}
